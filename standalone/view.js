class View {
	
	  constructor(fusekiURL) {
	  	this.fusekiURL = fusekiURL;
	  }
}

$( document ).ready(function() {
    var fusekiURL = "http://deep-thought.slis.uiowa.edu:3030/"
	var viewObj = new View();
}